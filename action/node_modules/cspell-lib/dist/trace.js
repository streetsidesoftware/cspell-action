"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.traceWords = void 0;
const gensequence_1 = require("gensequence");
const Settings_1 = require("./Settings");
const LanguageSettings_1 = require("./Settings/LanguageSettings");
const SpellingDictionary_1 = require("./SpellingDictionary");
const util = __importStar(require("./util/util"));
async function traceWords(words, settings, options) {
    const { languageId, locale: language, ignoreCase = true } = options || {};
    async function finalize(config) {
        var _a;
        const withLocale = Settings_1.mergeSettings(config, { language });
        const withLanguageId = LanguageSettings_1.calcSettingsForLanguageId(withLocale, (_a = languageId !== null && languageId !== void 0 ? languageId : withLocale.languageId) !== null && _a !== void 0 ? _a : 'plaintext');
        const settings = Settings_1.finalizeSettings(withLanguageId);
        const dictionaries = (settings.dictionaries || [])
            .concat((settings.dictionaryDefinitions || []).map((d) => d.name))
            .filter(util.uniqueFn);
        const dictSettings = { ...settings, dictionaries };
        const dictBase = await SpellingDictionary_1.getDictionary(settings);
        const dicts = await SpellingDictionary_1.getDictionary(dictSettings);
        const activeDictionaries = dictBase.dictionaries.map((d) => d.name);
        return {
            activeDictionaries,
            config: settings,
            dicts,
        };
    }
    const { config, dicts, activeDictionaries } = await finalize(settings);
    const setOfActiveDicts = new Set(activeDictionaries);
    const opts = { ignoreCase, useCompounds: config.allowCompoundWords };
    const r = await Promise.all(gensequence_1.genSequence(words)
        // Combine the words with the configs
        .map((word) => ({ word, config, dicts }))
        .toArray());
    function normalizeErrors(errors) {
        if (!(errors === null || errors === void 0 ? void 0 : errors.length))
            return undefined;
        return errors;
    }
    // Search each dictionary for the word
    const s = gensequence_1.genSequence(r)
        .concatMap((p) => {
        const { word, config, dicts } = p;
        return dicts.dictionaries
            .map((dict) => ({ dict, findResult: dict.find(word, opts) }))
            .map(({ dict, findResult }) => {
            var _a;
            return ({
                word,
                found: !!(findResult === null || findResult === void 0 ? void 0 : findResult.found),
                foundWord: (findResult === null || findResult === void 0 ? void 0 : findResult.found) || undefined,
                forbidden: (findResult === null || findResult === void 0 ? void 0 : findResult.forbidden) || false,
                noSuggest: (findResult === null || findResult === void 0 ? void 0 : findResult.noSuggest) || false,
                dictName: dict.name,
                dictSource: dict.source,
                dictActive: setOfActiveDicts.has(dict.name),
                configSource: config.name || '',
                errors: normalizeErrors((_a = dict.getErrors) === null || _a === void 0 ? void 0 : _a.call(dict)),
            });
        });
    })
        .toArray();
    return s;
}
exports.traceWords = traceWords;
//# sourceMappingURL=trace.js.map
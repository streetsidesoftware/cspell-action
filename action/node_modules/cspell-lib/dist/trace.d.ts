import { CSpellSettings } from './Settings';
export interface TraceResult {
    word: string;
    found: boolean;
    dictName: string;
    dictSource: string;
    configSource: string;
}
export declare function traceWords(words: string[], settings: CSpellSettings): Promise<TraceResult[]>;

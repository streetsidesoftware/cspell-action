"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extendExpect = exports.toEqualCaseInsensitive = void 0;
const toEqualCaseInsensitive = 
// `floor` and `ceiling` get types from the line above
// it is recommended to type them as `unknown` and to validate the values
function (actual, expected) {
    if (typeof actual !== 'string' || typeof expected !== 'string') {
        throw new Error('These must be of type number!');
    }
    const pass = actual.toLowerCase() === expected.toLowerCase();
    return {
        message: () => 
        // `this` context will have correct typings
        `expected ${this.utils.printReceived(actual)} to equal ${this.utils.printExpected(expected)} case insensitive`,
        pass,
    };
};
exports.toEqualCaseInsensitive = toEqualCaseInsensitive;
function extendExpect(e) {
    e.extend({
        toEqualCaseInsensitive: exports.toEqualCaseInsensitive,
    });
    return e;
}
exports.extendExpect = extendExpect;
//# sourceMappingURL=test.matchers.js.map
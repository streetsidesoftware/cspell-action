import { cleanCopy } from '../utils/util.js';
const defaultEditCosts = {
    accentCosts: 1,
    baseCost: 100,
    capsCosts: 1,
    firstLetterPenalty: 4,
    nonAlphabetCosts: 110,
};
const defaultHunspellCosts = {
    ...defaultEditCosts,
    ioConvertCost: 30,
    keyboardCost: 99,
    mapCost: 25,
    replaceCosts: 75,
    tryCharCost: 100,
};
export function mapHunspellCosts(costs = {}) {
    return { ...defaultHunspellCosts, ...cleanCopy(costs) };
}
export function mapEditCosts(costs = {}) {
    return { ...defaultEditCosts, ...cleanCopy(costs) };
}

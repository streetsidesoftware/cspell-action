const SymEmpty = Symbol('memorizeLastCall');
export function memorizeLastCall(fn) {
    let lastP = undefined;
    let lastR = SymEmpty;
    function calc(p) {
        if (lastR !== SymEmpty && lastP === p)
            return lastR;
        lastP = p;
        lastR = fn(p);
        return lastR;
    }
    return calc;
}
